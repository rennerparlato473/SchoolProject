// Exercise 1: Define a function to calculate the sum of two numbers
function addNumbers(a, b) {
    return a + b;
}

// Exercise 2: Create a variable to store the result of adding 5 and 7
let result = addNumbers(5, 7);

// Exercise 3: Use an if-else statement to check if a number is even or odd
const num = parseInt(prompt("Enter a number: "));

if (num % 2 === 0) {
    console.log(`The number ${num} is even.`);
} else {
    console.log(`The number ${num} is odd.`);
}
